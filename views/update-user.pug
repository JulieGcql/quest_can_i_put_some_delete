extends layout

block content
    form(method='POST' action="?_method=PUT")
        div.form-group
            label(for='name') Update name:
            input#name.form-control(type='text' placeholder='ton nom' name='name')
        button.btn.btn-primary(type='submit') UPDATE